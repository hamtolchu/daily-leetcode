// https://leetcode.com/problems/smallest-string-with-swaps/

function solution(s: string, pairs: number[][]) {
  // const strSplit = s.split();
  console.log(pairs);
  console.log(`${s[0]} ${s[0].charCodeAt(0)}`);
}


solution("dcab", [[0,3],[1,2],[0,2]]);

export default solution;